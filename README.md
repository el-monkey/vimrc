# My Vimrc File Repository

- Clone this repository.

```bash
    $ git clone git@github.com:el-monkey/vimrc.git
```

- Create a symlink to the .vimrc file in home directory.

```bash
    $ ln -sf ~/Development/vimrc/.vimrc ~/.vimrc
```
